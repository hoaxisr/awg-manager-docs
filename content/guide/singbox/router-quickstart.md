---
title: Sing-box Router — пример настройки
weight: 3
---

Простая пошаговая инструкция: настроим Sing-box Router так, чтобы трафик к **RuTube** шёл через выбранный туннель. Типичный случай — открыть RuTube из-за пределов России, где сервис ограничивает доступ; та же схема подойдёт для любого другого сервиса, у которого есть готовый geosite-набор.

В примере используем публичный rule_set из репозитория **SagerNet/sing-geosite** — он содержит все домены RuTube в готовом srs-формате, поэтому в Sing-box Router его можно подключить одной строкой.

## Что должно быть готово

- Sing-box установлен (см. [обзор раздела Sing-box](../)).
- Уровень использования — **Продвинутый**, чтобы появилась вкладка **Sing-box Router** (см. [Настройки → Уровни использования](../../settings/#уровни-использования)).
- В **Sing-box Router → Движок** включён движок и выбрана **NDMS Access Policy** с привязанным целевым устройством (то самое, с которого вы будете открывать RuTube).
- В системе есть туннель, через который пойдёт RuTube — обычный AmneziaWG-туннель, sing-box-туннель или [подписка](subscriptions/). В примере — `awg-sys-Wireguard0`.

## Шаг 1 — добавить rule_set с доменами RuTube

Откройте **Sing-box Router → Наборы** и нажмите **+ Набор**. В модалке заполните:

- **Tag** — `geosite-rutube` (под этим именем набор будет видно в правилах).
- **Тип** — `remote` (sing-box сам скачает файл и будет обновлять по расписанию).
- **URL** — `https://github.com/SagerNet/sing-geosite/raw/refs/heads/rule-set/geosite-rutube.srs`
- **Формат** — `binary` (для `.srs`-файлов).
- **Интервал обновления** — оставьте `24h`.

Сохраните. В подразделе **Наборы** появится строка `geosite-rutube`.

<!-- TODO-SCREENSHOT: rule-set-add.png — модалка добавления rule_set с заполненными полями URL и Tag. -->
![Добавление rule_set RuTube](/img/guide/singbox/router-quickstart/rule-set-add.png)

## Шаг 2 — добавить правило маршрутизации

Откройте **Sing-box Router → Правила** и нажмите **+ Правило**.

- **Действие** — `route` (направить трафик в туннель).
- **Matchers → Rule sets** — выберите `geosite-rutube` из списка.
- **Outbound** — туннель, через который пойдёт RuTube. В примере `awg-sys-Wireguard0`.

Сохраните. В таблице правил появится строка `ROUTE` с матчером `set: geosite-rutube` и outbound'ом вашего туннеля.

<!-- TODO-SCREENSHOT: rule-add.png — модалка добавления правила маршрутизации. -->
![Добавление правила маршрутизации](/img/guide/singbox/router-quickstart/rule-add.png)

## Шаг 3 — направить DNS-запросы RuTube через туннель

Без этого шага sing-box будет резолвить домены RuTube через ваш обычный DNS Keenetic — а тот, скорее всего, возвращает IP с географической привязкой к вашему текущему расположению. Для надёжной работы нужен DNS-сервер на стороне туннеля.

Откройте **Sing-box Router → DNS**. Сначала добавим внешний DNS-сервер кнопкой **+ Сервер**:

- **Tag** — `rutube-dns`.
- **Тип** — `UDP`.
- **Адрес** — `1.1.1.1:53` (или любой другой публичный DNS — Cloudflare, Google `8.8.8.8`, Yandex `77.88.8.8`).
- **Detour** — выберите тот же туннель, через который пойдёт RuTube (`awg-sys-Wireguard0`). Без этого DNS-запрос пойдёт мимо туннеля и снова вернёт "не тот" IP.

<!-- TODO-SCREENSHOT: dns-server-add.png — модалка добавления DNS-сервера с detour через туннель. -->
![Добавление DNS-сервера](/img/guide/singbox/router-quickstart/dns-server-add.png)

Теперь добавьте DNS-правило сопоставления — **+ Правило** в секции DNS-правил:

- **Действие** — `resolve`.
- **Rule set** — `geosite-rutube` (тот же набор, что и для маршрутизации).
- **Server** — `rutube-dns`.

Сохраните. В таблице DNS-правил появится строка `RESOLVE` с матчером `rule_set: geosite-rutube` и сервером `rutube-dns`.

<!-- TODO-SCREENSHOT: dns-rule-add.png — модалка добавления DNS-правила. -->
![Добавление DNS-правила](/img/guide/singbox/router-quickstart/dns-rule-add.png)

## Шаг 4 — применить изменения

После всех изменений в конфигурации Sing-box Router (нового rule_set, правила маршрутизации, DNS-сервера, DNS-правила) нажмите кнопку **Применить**. Без этого новые настройки сохраняются в конфигурации, но не вступают в силу в работающем sing-box — трафик продолжит идти по старым правилам.

Кнопка **Применить** появляется, когда в конфигурации есть несохранённые правки, и подсвечивается, чтобы напомнить о необходимости применить изменения.

<!-- TODO-SCREENSHOT: apply-button.png — кнопка Применить с индикатором несохранённых изменений. -->
![Применение изменений](/img/guide/singbox/router-quickstart/apply-button.png)

## Шаг 5 — проверить

На целевом устройстве (то, что привязано к Access Policy движка) откройте RuTube. Видео должно загружаться без географической блокировки.

Если что-то не работает — диагностика:

- **Sing-box Router → Соединения** — там видно живые соединения от вашего устройства. У запросов к доменам RuTube outbound должен быть тот туннель, который вы указали в правиле. Если outbound `direct` — правило не сработало, проверьте matcher.
- **Диагностика → Журнал** — включите буфер sing-box на уровень `DEBUG` и посмотрите, как sing-box распознаёт DNS-запросы. В логах должны мелькать строчки про `geosite-rutube` и резолв через `rutube-dns`.

<!-- TODO-SCREENSHOT: connections-check.png — вкладка Соединения с активными соединениями к RuTube через выбранный outbound. -->
![Проверка соединений](/img/guide/singbox/router-quickstart/connections-check.png)

## Что дальше

Та же схема работает для любого другого сервиса с готовым набором в репозиториях **SagerNet/sing-geosite** или **SagerNet/sing-geoip** — меняется только URL rule_set'а и tag в matcher'е правила. Для самых популярных сценариев есть [Пресеты](../router/#presets), где нужные правила и rule_set'ы собраны под одну кнопку, там вы можете добавить Vkontakte, Ozon, WildBerries и другие сервисы полюбившиеся вам.
